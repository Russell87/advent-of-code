# Get the input file and go line by line
# Iterate through each line as an array
# Separate each line by l w h
#
# Surface area: 2*l*w + 2*w*h + 2*h*l



file = 'input.txt'
File.readlines(file).each do |line|
  puts line
end